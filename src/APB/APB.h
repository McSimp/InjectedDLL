#pragma once

namespace CryptHook
{
    void Initialise();
    void Shutdown();
}

namespace NetworkHook
{
    void Initialise();
    void Shutdown();
}

namespace SRPHook
{
    void Initialise();
    void Shutdown();
}

namespace CompressionHook
{
    void Initialise();
    void Shutdown();
}
